defmodule Apix.SchemaTest do
  use ExUnit.Case, async: true

  alias Schema

  describe "#{inspect(Schema)} scalars" do
    test "nil" do
    end

    test "boolean()" do
    end

    test "atom()" do
    end

    test "integer()" do
    end

    test "float()" do
    end

    test "string()" do
    end

    test "reference()" do
    end

    test "process()" do
    end

    test "port()" do
    end
  end

  describe "#{inspect(Schema)} collection" do
    test "tuple" do
    end

    test "list" do
    end

    test "map" do
    end

    test "struct" do
    end

    test "exception" do
    end
  end

  describe "#{inspect(Schema)} function" do
    test "function()" do
    end
  end

  describe "#{inspect(Schema)} " do
  end
end
